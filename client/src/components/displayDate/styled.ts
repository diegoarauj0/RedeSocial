import styled from "styled-components";

export default {
    DisplayDate:styled.p`
        color: ${props => props.theme.color3};
        font-weight: 600;
        font-family: ${props => props.theme.SegoeUI};
        font-size: 14px;
    `
}
